# Task 05 - Criar estrutura de dominio orders

## Objetivo
Criar o container de dominio `libs/orders`.

## Comandos
```bash
mkdir -p libs/orders
```

## Descricao detalhada
- Cria o diretório raiz do dominio `orders`.
- Ainda sem projeto Nx; apenas prepara a hierarquia de negocio.

## Resultado esperado
- Pasta `libs/orders/` existente.

## Criterio de aceite
- `libs/orders` visivel no workspace.

## Erros comuns e correcao
- Em PowerShell, se `mkdir -p` falhar, usar `New-Item -ItemType Directory -Path libs/orders -Force`.

## Checkpoint
Se aceito, ir para `task-06-create-users-domain-structure.md`.

